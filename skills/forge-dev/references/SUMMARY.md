# SUMMARY: <T01 - 任务名>

- **Change ID**: <id>
- **Task ID**: <T01>
- **完成时间**: <YYYY-MM-DD HH:mm>
- **AI 角色**: Dev

---

## 做了什么（一段话）

<实际完成的事，避免照抄 TASK.md action。重点：实际选择 / 偏离原计划的地方 / 遗留。>

## 改动文件

| 文件 | 性质 | 说明 |
|---|---|---|
| | | |

## verify 输出（必填）

```text
<贴出真实跑过的 verify 命令输出。失败也贴。>
```

## 6 维自查（生产代码改动必填）

> 装了 brooks-lint → `/brooks-review` 输出原样贴入；未装 → AI 内置 R1~R6 快查结果。

```markdown
<4 要素格式：>
### 🔴/🟡/🟢 R<x> · <名字>：<结论>
**Symptom**：...
**Source**：...
**Consequence**：...
**Remedy**：...
```

### 已知接受 + 理由（🟡 Major 项不修的）

- <列出 + 理由 + 计划何时修>

### 已知小问题（🟢 Minor 项可省的）

- <列出>

## 数据库迁移（涉及 schema 变更必填）

> 无 schema 变更可写 N/A。

### Schema Diff

```
- 新增表：...
- 改字段：...
```

### 迁移文件

| 文件 | 框架 | 含 up | 含 down |
|---|---|---|---|
| | | | |

### 执行状态

- [ ] 已检测 DB 凭据
- [ ] 已反问用户（找到凭据时）
- [ ] 执行结果（用户选"现在执行"时填）
- [ ] 结构验证（用户选"现在执行"时填）

### 提醒（用户选"只生成 SQL" 或未检测到凭据时必填）

```
数据库迁移文件已生成，但未在任何环境执行。
  - 文件：...
  - 执行命令：...
  - 验证命令：...
  - 环境清单：
    - [ ] local
    - [ ] dev
    - [ ] staging
    - [ ] prod
```

## 越界检查（必填 · R6.5）

```
✅ 越界检查（R6.5）：
  - TASK write_files：<N> 项
  - 实际 diff 涉及：<M> 项
  - 越界：0 / 已撤销 / 已扩范围
```

## 破坏性变更（涉及破坏性改动必填）

> 不涉及可写 N/A。

### 判定命中

- [ ] 删除既有代码 >= 5 行
- [ ] 改公共导出签名
- [ ] 改公共 API
- [ ] 删除文件 / 重命名导出符号

### 引用图（必填 · grep 结果）

```
被删/改的符号：... (<file:line>)
直接调用：...
间接影响：...
```

### 反问用户结果

- 用户选择：...
- 同意人：...
- 时间：...

### 回归测试覆盖

- 旧路径测试位置：...
- 新路径测试位置：...
- 覆盖率证据：...

## 决策与偏离（如有）

- <偏离原因>
- <引入未在 DESIGN 中提到的依赖/模式原因>

## 是否触发新工作

- [ ] 触发新 fix-plan（已追加到 TASK.md）
- [ ] 触发 CONTEXT.md 更新（已更新）
- [ ] 发现需求/设计问题，已暂停并提交给人工

## 完成判定

- TASK.md 中对应任务已勾选：是
- 提交 hash：`<commit-sha>`
